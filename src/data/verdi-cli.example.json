{
  "_comment": "Schema only — two example commands shown. The real file (~26 KB, hundreds of commands) is generated by scripts/fetch-verdi-cli.py at build time and gitignored. This stays committed so the expected shape is documented in case the script is ever lost or aiida-core's CLI structure changes.",
  "commands": ["verdi archive", "verdi archive create"],
  "subcommands": {
    "verdi archive": [
      "create",
      "import",
      "info",
      "inspect",
      "migrate",
      "version"
    ]
  },
  "help": {
    "verdi archive": "Create, inspect and import AiiDA archives.",
    "verdi archive create": "Create an AiiDA archive."
  }
}
